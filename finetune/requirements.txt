# Isolated deps for the division-clustering fine-tune. Install into a DEDICATED
# venv so the repo-root uv.lock / pyproject.toml are never touched:
#
#   uv venv finetune/.venv --python 3.12
#   uv pip install --python finetune/.venv -r finetune/requirements.txt
#
# (Use `uv pip install -r`, NOT `uv sync`/`uv add`, so uv never rewrites the
# root lockfile.)
sentence-transformers>=3.0
datasets>=2.19
accelerate>=0.30
scikit-learn>=1.4

# Pinned explicitly so this venv is self-sufficient and independent of the
# ad-hoc torch/transformers already present in the root .venv.
torch>=2.2
transformers>=4.40
huggingface_hub>=0.23
numpy>=1.26
